# SNS 이미지 편집툴 기능 명세서

## 1. 문서 개요

### 1.1 목적
특정 SNS 비율 또는 사용자가 지정한 비율·사이즈의 캔버스에 이미지를 배치하고 편집한 뒤, 완성 이미지를 기기에 저장할 수 있는 이미지 편집툴을 정의한다.

### 1.2 핵심 원칙
- 캔버스 크기는 최종 출력 이미지의 기준이 된다.
- 편집 영역은 하단 레이어와 상단 레이어로 구성된다.
- 하단 레이어는 배경 표현과 배경 이미지 효과를 담당한다.
- 상단 레이어는 이미지 크기 조절만 지원한다.
- 캔버스 밖으로 벗어난 이미지는 출력 시 캔버스 영역만 반영한다.

## 2. 용어 정의

| 용어 | 정의 |
| --- | --- |
| 캔버스 | 선택한 비율과 사이즈를 기준으로 이미지를 편집하는 최종 출력 영역 |
| 하단 레이어 | 캔버스의 배경을 구성하는 레이어 |
| 상단 레이어 | 하단 레이어 위에 배치되는 이미지 레이어 |
| 기준선 | 이미지의 크기와 위치를 맞출 때 사용하는 캔버스의 가장자리 선 |
| 자석 맞춤 | 이미지 가장자리가 캔버스 기준선 근처에 도달하면 자동으로 기준선에 맞춰지는 동작 |
| 오버플로 | 이미지가 캔버스 경계를 넘어간 상태 |

## 3. 지원 규격

### 3.1 SNS 비율 프리셋
서비스에서 자주 사용하는 비율을 프리셋으로 제공한다. 프리셋은 운영 정책에 따라 추가·변경할 수 있다.

- 정사각형: 1:1
- 세로형: 4:5
- 세로형: 3:4 (1080 × 1440px)
- 세로형: 9:16
- 가로형: 16:9
- 사용자가 직접 지정하는 비율

프리셋에는 비율과 함께 기본 출력 사이즈를 연결할 수 있어야 한다.

### 3.2 사용자 지정 규격
사용자는 다음 값을 직접 입력할 수 있다.

- 가로 사이즈
- 세로 사이즈
- 단위: 기본 px
- 비율 잠금: 잠금 상태에서는 한쪽 값을 변경할 때 다른 쪽 값이 비율에 맞춰 변경됨

입력값은 1 이상의 정수만 허용하며, 서비스가 정한 최대 캔버스 크기를 초과할 수 없다. 잘못된 값은 편집 시작 전에 즉시 안내한다.

## 4. 사용자 흐름

1. 사용자가 SNS 비율 프리셋 또는 사용자 지정 비율·사이즈를 선택한다.
2. 시스템이 선택한 크기의 캔버스를 생성한다.
3. 사용자가 하단 레이어의 배경 유형을 선택한다.
4. 필요하면 하단 레이어에 배경 이미지를 삽입하고 효과와 밝기를 조정한다.
5. 필요하면 상단 레이어에 이미지를 삽입하고 크기를 조정한다.
6. 사용자가 편집 결과를 확인한다.
7. 시스템이 캔버스 영역만 이미지 파일로 렌더링한다.
8. 사용자가 완성 이미지를 기기에 저장한다.

## 5. 화면 구성

메인 레이아웃은 좌우 2열로 구성한다.

- 왼쪽 패널: 캔버스 규격, 레이어, 배경, 효과, 저장 등 설정 기능
- 오른쪽 영역: 캔버스와 편집 결과를 확인하는 편집 화면
- 데스크톱에서는 왼쪽 패널의 폭을 일정하게 유지하고, 오른쪽 편집 화면이 남은 공간을 사용한다.
- 화면 너비가 좁아지면 왼쪽 패널과 오른쪽 편집 화면을 위아래로 전환한다.

### 5.1 왼쪽 설정 패널

왼쪽 설정 패널은 편집에 필요한 설정과 조작 기능을 제공한다. 설정이 많아질 경우 패널 내부만 스크롤되며, 오른쪽 편집 화면은 가능한 영역을 유지한다.

#### 캔버스 규격 설정
- SNS 비율 프리셋 선택
- 사용자 지정 가로·세로 사이즈 입력
- 비율 잠금 토글
- 캔버스 생성 또는 적용 버튼

#### 레이어 조작
- 레이어 선택 UI는 오른쪽 편집 화면의 왼쪽 중앙에 배치한다.
- 상단 레이어 아이콘은 레이어 선택 UI의 위쪽에 배치한다.
- 하단 레이어 아이콘은 레이어 선택 UI의 아래쪽에 배치한다.
- 아이콘을 선택하면 왼쪽 설정 패널에 해당 레이어의 설정을 표시한다.
- 이미지 선택 및 교체
- 선택한 레이어의 이미지 제거 또는 초기화
- 선택된 이미지가 있는 레이어의 제거 버튼 표시

#### 저장
- 저장 버튼
- 저장 상태 및 오류 메시지

### 5.2 오른쪽 편집 화면

오른쪽 편집 화면은 설정 패널에서 선택한 캔버스와 레이어 상태를 시각적으로 표시한다.

- 실제 출력 비율을 유지하는 캔버스
- 하단 레이어와 상단 레이어의 시각적 구분
- 선택된 이미지의 크기 조절 핸들
- 캔버스 기준선
- 오버플로 상태를 나타내는 점선 테두리

편집 화면은 화면 크기에 맞춰 축소·확대해 표시할 수 있지만, 내부 좌표와 최종 출력 해상도는 설정된 캔버스 크기를 유지한다.

#### 편집 화면 상태
- 이미지가 선택된 경우 해당 이미지의 조절 핸들을 표시한다.
- 이미지가 캔버스 경계를 넘으면 넘친 방향의 캔버스 테두리를 점선으로 표시한다.
- 저장 시에는 편집 화면의 보조 UI를 제외하고 캔버스 내부만 출력한다.
- 레이어 아이콘 선택 UI는 편집 보조 UI이며 최종 저장 이미지에 포함하지 않는다.

#### 스테이지 보기 조작
- 스테이지 배경색은 `#efefef`로 표시한다. 이 색상은 출력 이미지의 배경색과 별개다.
- `Ctrl + 마우스 휠`로 편집 화면의 표시 배율을 확대·축소한다.
- 확대·축소 범위는 기본적으로 50%에서 400% 사이로 제한한다.
- 확대·축소 시 마우스 포인터가 가리키는 위치를 기준으로 화면을 조정한다.
- 마우스 휠 버튼을 누른 채 드래그하면 편집 화면을 이동(패닝)한다.
- 보기 배율과 패닝은 편집 화면 표시 상태에만 적용하며, 최종 저장 이미지의 해상도와 좌표에는 영향을 주지 않는다.
- 화면 맞춤 버튼을 누르면 배율을 100%, 이동 위치를 초기 상태로 되돌린다.

## 6. 하단 레이어

하단 레이어의 배경 유형은 다음 세 가지 중 하나다.

### 6.1 색상 배경
- 색상 선택기 제공
- 선택한 색상을 캔버스 전체에 적용
- 기본값은 서비스가 정한 기본 색상으로 설정

### 6.2 투명 배경
- 캔버스 배경을 투명으로 설정
- 저장 시 투명도를 지원하는 파일 형식을 사용해야 한다.
- 투명 배경을 지원하지 않는 형식으로 저장하는 경우 사용할 대체 배경 정책을 별도로 정의한다.

### 6.3 이미지 배경
- 기기에서 이미지 파일 선택
- 이미지 크기와 위치 조절
- 이미지가 캔버스보다 크거나 작아도 허용
- 이미지 모서리 핸들을 드래그하여 크기 조절
- 캔버스 기준선 근처에서 자석 맞춤 적용
- 캔버스 밖으로 벗어난 부분은 오버플로로 표시
- 최종 저장 시 캔버스 바깥 영역은 제외

#### 적용 가능한 효과
배경 이미지에만 다음 효과를 적용한다.

- 블러: 수치 입력 또는 슬라이더로 강도 조절
- 모자이크: 수치 입력 또는 슬라이더로 강도 조절
- 밝기: 효과와 독립적으로 수치 조절

블러와 모자이크의 범위, 기본값, 최대값은 제품 정책으로 정한다. 효과 값 변경은 편집 화면에 즉시 반영되어야 하며, 효과를 적용해도 원본 파일 자체는 변경하지 않는다.

## 7. 상단 레이어

- 이미지 삽입 및 교체
- 이미지 크기 조절
- 이미지 모서리 드래그를 통한 크기 조절
- 캔버스보다 크게 또는 작게 조절 가능
- 캔버스 기준선 근처에서 자석 맞춤 적용
- 오버플로 상태에서 캔버스 경계에 점선 표시

상단 레이어에는 블러, 모자이크, 밝기 등 이미지 효과를 제공하지 않는다. 상단 레이어의 편집 권한은 크기 조절로 제한한다.

## 8. 크기 조절 및 자석 맞춤

### 8.1 조절 방식
- 선택 이미지의 네 모서리에 크기 조절 핸들을 표시한다.
- 핸들을 드래그하면 이미지 비율을 유지한 채 크기를 변경한다.
- 일반 모서리 드래그는 반대쪽 모서리를 기준으로 크기를 변경한다.
- `Ctrl + 모서리 드래그`는 이미지 중심을 기준으로 크기를 변경한다.
- 최소 크기는 1px 이상으로 제한한다.
- 이미지가 캔버스 바깥으로 이동하거나 확대되는 상태를 허용한다.

### 8.2 자석 맞춤
- 이미지 가장자리와 캔버스 가장자리의 거리가 설정된 자석 범위 이내이면 가장자리를 캔버스 기준선에 맞춘다.
- 맞춤 대상은 좌측, 우측, 상단, 하단 캔버스 경계다.
- 드래그 중에는 맞춤 예정 기준선을 시각적으로 표시할 수 있다.
- 사용자가 계속 드래그하면 자석 맞춤을 해제하고 자유 조절을 허용한다.

### 8.3 오버플로 표시
- 이미지가 캔버스의 어느 한 변이라도 넘어가면 해당 캔버스 테두리를 점선으로 표시한다.
- 이미지가 캔버스 안으로 돌아오면 해당 테두리의 점선 표시를 제거한다.
- 점선은 편집 보조 표시이며 최종 저장 이미지에는 포함하지 않는다.
- 오버플로 여부는 하단·상단 레이어에 동일하게 적용한다.

## 8.4 이미지 삭제

- 이미지가 활성화된 상태에서 `Delete` 키를 누르면 선택된 레이어의 이미지를 삭제한다.
- 키보드를 사용할 수 없는 경우 설정 패널의 이미지 제거 버튼으로 동일하게 삭제할 수 있다.
- 하단 레이어 이미지를 삭제하면 하단 레이어는 색상 배경으로 전환한다.
- 상단 레이어 이미지를 삭제하면 상단 레이어는 빈 상태가 된다.
- 텍스트·숫자·색상 등 입력 컨트롤에 포커스가 있을 때는 `Delete` 키로 이미지를 삭제하지 않는다.

## 9. 저장

### 9.1 저장 동작
- 저장 버튼을 누르면 캔버스 내부 영역만 렌더링한다.
- 하단 레이어를 먼저 렌더링하고 상단 레이어를 그 위에 렌더링한다.
- 점선, 선택 테두리, 조절 핸들 등 편집용 UI는 제외한다.
- 편집 중인 효과값과 이미지 배치를 저장 결과에 반영한다.
- 브라우저 또는 앱 환경에 맞는 기기 저장 방식을 사용한다.

### 9.2 저장 파일
- 기본 파일 형식: PNG 또는 서비스에서 정한 이미지 형식
- 파일명 기본값: `sns-image-{날짜·시간}`
- 투명 배경을 보존해야 하는 경우 PNG를 사용한다.
- 저장 성공과 실패를 사용자에게 명확히 안내한다.

## 10. 상태 및 예외 처리

- 이미지가 선택되지 않은 상태에서도 색상 배경과 투명 배경을 사용할 수 있어야 한다.
- 지원하지 않는 파일 형식은 선택 단계에서 거부한다.
- 파일 용량 또는 이미지 해상도가 제한을 초과하면 안내 메시지를 표시한다.
- 이미지 로딩 중에는 로딩 상태를 표시하고 편집 동작을 제한한다.
- 저장 권한이 없거나 저장에 실패하면 원인을 안내하고 다시 시도할 수 있어야 한다.
- 사용자가 캔버스 규격을 변경하면 기존 이미지 배치를 유지할지 초기화할지 정책을 정하고 일관되게 적용한다.

## 11. 접근성 및 사용성

- 모든 입력값과 조작 기능은 키보드로 접근할 수 있어야 한다.
- 색상 선택은 색상값 입력 또는 대체 텍스트를 제공한다.
- 점선 표시만으로 오버플로 상태를 전달하지 않고 상태 텍스트나 보조 안내를 함께 제공한다.
- 모바일 화면에서도 캔버스와 주요 조작 버튼이 화면 밖으로 잘리지 않아야 한다.
- 수치 입력에는 현재값, 최소값, 최대값을 확인할 수 있어야 한다.

## 12. 기능 검수 기준

- 프리셋을 선택하면 정의된 비율의 캔버스가 생성된다.
- 3:4 프리셋을 선택하면 `1080 × 1440px` 캔버스가 생성된다.
- 사용자 지정 가로·세로 사이즈가 캔버스와 저장 결과에 정확히 반영된다.
- 하단 레이어에서 색상, 투명, 이미지 배경을 각각 선택할 수 있다.
- 하단 이미지에 블러, 모자이크, 밝기를 각각 조정할 수 있다.
- 상단 레이어는 이미지 크기 조절만 가능하다.
- 이미지가 캔버스보다 크거나 작게 조절된다.
- 이미지 모서리 드래그로 크기를 조절할 수 있다.
- 캔버스 경계 근처에서 이미지가 자석처럼 맞춰진다.
- 이미지가 캔버스 밖으로 나가면 해당 경계가 점선으로 표시된다.
- 선택한 이미지를 `Delete` 키와 설정 패널의 제거 버튼으로 각각 삭제할 수 있다.
- `Ctrl + 마우스 휠`로 편집 화면을 확대·축소할 수 있다.
- 마우스 휠 버튼 드래그로 확대된 편집 화면을 이동할 수 있다.
- 저장 결과에 편집 UI와 캔버스 바깥 이미지가 포함되지 않는다.
- 투명 배경 선택 시 지원 파일 형식으로 투명도가 보존된다.
- 데스크톱과 모바일 화면에서 주요 편집·저장 흐름을 완료할 수 있다.

## 13. 데이터 모델 예시

```text
Canvas {
  width: number,
  height: number,
  presetId: string | null
}

StageView {
  zoom: number,
  panX: number,
  panY: number
}

BottomLayer {
  type: "color" | "transparent" | "image",
  color: string | null,
  image: ImageAsset | null,
  transform: Transform,
  effects: {
    blur: number,
    mosaic: number,
    brightness: number
  }
}

TopLayer {
  image: ImageAsset | null,
  transform: Transform
}

Transform {
  x: number,
  y: number,
  width: number,
  height: number
}
```

## 14. 범위 제외

다음 기능은 본 문서의 기본 범위에 포함하지 않는다.

- 이미지 회전 및 기울이기
- 이미지 자르기 도구
- 여러 개의 상단 레이어 추가
- 텍스트, 스티커, 도형 편집
- 상단 레이어의 색상·효과 보정
- 편집 프로젝트의 서버 저장 및 협업
